export function ChaosBadge({ children }: { children: string }) {
  return <span className="border border-border-strong bg-background px-2 py-1 font-mono text-[11px] uppercase tracking-[0.12em] text-muted-foreground">{children}</span>;
}
